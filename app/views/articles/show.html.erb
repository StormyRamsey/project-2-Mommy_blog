<!DOCTYPE html>
<html>
<header>

</header>
  <body>


<div class='article-box'>
  <h3><%= @article.title %></h3>
      <img class="img-edit" src="<%= @article.img_url %>"/>
      <div class='review-post'>
      <p><%= @article.text %></p>
      <p><%= link_to 'Edit Post', edit_article_path(@article) %></p>
    </div>
</div>

<div class='comment-box'>
  <div class='comments'>
      <h3>Comments</h3>
        <% @article.comments.each do |comment| %>
          <p><%= comment.commenter %></p>
          <p><%= comment.body %></p>
            <p><%= link_to 'Edit', edit_article_comment_path(@article, comment) %>
                <%= link_to 'Delete', [comment.article, comment],
                         method: :delete,
                         data: { confirm: 'Are you sure, you want to delete this comment?' } %></p>
        <% end %>

          <h3>Add a comment:</h3>
            <%= form_for([@article, @article.comments.build]) do |f| %>
              <p><%= f.label :name%><br>
              <%= f.text_field :commenter %></p>
              <p><%= f.label :comment %><br></p>
              <p><%= f.text_area :body %> <%= f.submit :value => "Post Comment", :class => "submit"%></p>
              <% end %>

    </div>
  </div>


</body>
</html>
